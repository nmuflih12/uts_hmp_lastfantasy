import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uts_hmp_lastfantasy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
